# Example environment / config values
# Keep placeholder values only. Never commit real secrets.

APP_ENV=development
APP_PORT=3000
DATABASE_URL=postgres://user:password@localhost:5432/app_db
API_BASE_URL=https://api.example.com
API_KEY=replace-me
JWT_SECRET=replace-me
